<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ezria.lifetrackr.Mapper.BillMapper">


    <insert id="insertBatch">
        insert into bill (userid, counterparty, goods, income, money, trading_hours)
        values
        <foreach collection="billList" item="item" separator=",">
            (#{item.userid}, #{item.counterparty}, #{item.goods}, #{item.income}, #{item.money}, #{item.tradingHours})
        </foreach>
    </insert>

    <select id="selectBillByPage" resultType="ezria.lifetrackr.VO.BillVO">
        select id, counterparty, goods, income, money, trading_hours from bill
        where userid = #{userid}
        <if test="income != null">and income = #{income}</if>
        ORDER BY trading_hours DESC
        LIMIT #{offset}, #{pageSize}
    </select>
</mapper>