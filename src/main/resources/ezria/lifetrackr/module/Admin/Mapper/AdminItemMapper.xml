<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ezria.lifetrackr.module.Admin.Mapper.AdminItemMapper">
    <delete id="deleteByIds">
        DELETE FROM item WHERE ID IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>


    <select id="selectItemByPage" resultType="ezria.lifetrackr.module.Admin.VO.AdminItemVO">
        SELECT item.ID,`name`,type,title,cover,description,rating,`status`,created_at,updated_at FROM `item`,`user`
        WHERE user_id = user.ID
        LIMIT #{offset}, #{pageSize}
    </select>
</mapper>